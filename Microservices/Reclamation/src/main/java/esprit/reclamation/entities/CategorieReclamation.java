package esprit.reclamation.entities;

/**
 * Catégorie métier déduite du texte (titre + description).
 */
public enum CategorieReclamation {
    FACTURATION,
    TECHNIQUE,
    QUALITE_SERVICE,
    ACCES_COMPTE,
    AUTRE
}
